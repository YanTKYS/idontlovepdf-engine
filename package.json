{
  "name": "@idontlovepdf/engine",
  "version": "0.4.2",
  "description": "Offline, browser-native engine for replacing text in existing PDF content streams",
  "type": "module",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "import": "./src/index.js"
    }
  },
  "files": [
    "src",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "pretest": "npm run build",
    "test": "node --test \"test/*.test.js\"",
    "pretest:no-subtle": "npm run build",
    "test:no-subtle": "node --import ./test/helpers/no-web-crypto.js --test \"test/*.test.js\"",
    "test:font": "node scripts/fetch-test-font.js",
    "pretest:browser": "npm run build",
    "test:browser": "node --test \"test/browser/*.test.js\"",
    "check": "for file in src/*.js src/security/*.js scripts/*.js web/*.js test/*.js test/helpers/*.js test/browser/*.js; do node --check \"$file\"; done",
    "build": "node scripts/build.js",
    "sync-version": "node scripts/sync-version.js",
    "assess:corpus": "node scripts/assess-corpus.js"
  },
  "keywords": [
    "pdf",
    "offline",
    "browser",
    "text-editing"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "1.56.0"
  },
  "dependencies": {
    "@noble/hashes": "1.8.0",
    "opentype.js": "^1.3.4"
  }
}
